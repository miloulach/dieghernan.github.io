language: ruby
rvm:
  - 2.5
  - 2.6
  - jruby
  - truffleruby

script:
- JEKYLL_ENV=production bundle exec jekyll build --destination site

deploy:
  provider: pages
  local_dir: ./site
  target_branch: master
  skip_cleanup: true
